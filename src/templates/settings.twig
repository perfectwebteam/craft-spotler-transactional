{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: "Client ID",
    id: 'clientId',
    name: 'clientId',
    instructions: "You can find this in your Spotler Transactional settings.",
    suggestEnvVars: true,
    value: adapter.clientId,
    errors: adapter.getErrors('clientId'),
}) }}

{{ forms.autosuggestField({
    label: "Client SECRET",
    id: 'clientSecret',
    name: 'clientSecret',
    instructions: "You can find this in your Spotler Transactional settings.",
    suggestEnvVars: true,
    value: adapter.clientSecret,
    errors: adapter.getErrors('clientSecret'),
}) }}

{{ forms.autosuggestField({
    label: "Subaccount",
    instructions: "If you want to send mail using a [subaccount], enter its unique identifier here.",
    id: 'subaccount',
    name: 'subaccount',
    suggestEnvVars: true,
    value: adapter.subaccount,
    errors: adapter.getErrors('subaccount')
}) }}

{{ forms.autosuggestField({
    label: "Template Slug",
    instructions: "If you want to send mail using a [template], enter its unique slug here.",
    id: 'template',
    name: 'template',
    suggestEnvVars: true,
    value: adapter.template,
    errors: adapter.getErrors('template')
}) }}